#!/bin/sh

sudo apt update # Ensure package list is up-to-date

sudo apt install qemu-system # Install QEMU, an open source, full system emulator

sudo apt install nasm # Netwide Assembler

sudo apt install gcc # GCC: The GNU compiler
